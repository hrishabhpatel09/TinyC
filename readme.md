# TinyC
A simple C parser and interpreter for a subset of the C programming language, implemented using Flex and Bison.

- To Build the parser(bin executable)
```bash
make all
```

- to build and run
```bash
make run
```